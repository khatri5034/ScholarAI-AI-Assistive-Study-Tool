# ScholarAI — copy to .env and fill in values

# Backend
OPENAI_API_KEY=sk-...
# PostgreSQL (required)
DATABASE_URL=postgresql://user:password@localhost:5432/scholarai

# Optional
ANTHROPIC_API_KEY=
PORT=8000

# Frontend — copy to frontend/.env.local
NEXT_PUBLIC_API_URL=http://localhost:8000
