"""
Embedding scripts — Read docs from documents/, chunk, embed via OpenAI (or local),
store vectors in vectors.db (e.g. ChromaDB / SQLite + vector extension).
Run after upload or on schedule.
"""
# TODO: load PDFs, split into chunks, call models.embed, write to embeddings/vectors.db
