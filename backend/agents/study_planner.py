"""
Study Planner Agent — Generates study plans (schedule, topics, deadlines).
Uses LLM (models/) and optionally course context from RAG.
"""
# TODO: define agent workflow, call LLM, return structured plan for DB
