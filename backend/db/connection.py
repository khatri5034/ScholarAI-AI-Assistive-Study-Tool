"""
Database connection — Postgres (or SQLite for dev).
Used by API routes to store users, study_plans, documents metadata.
"""
# TODO: connection pool, get_cursor, run migrations from database/schema.sql
